groups:
  - name: production_log_alerts
    rules:
      - alert: LogErrorDetected
        expr: count_over_time({host="production"} |= "ERROR" [1m]) > 0
        for: 0m
        labels:
          severity: critical
          source: loki
        annotations:
          summary: "Error Log detected on Production"